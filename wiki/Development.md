# Development

Use Node.js 24 and npm 11.

```bash
npm ci
npm run check
```

The deployable Screeps bundle is `dist/main.js`. Bootstrap CI does not deploy or require secrets.

Architecture and strategy changes update both versioned docs and the corresponding Wiki source.
